server:
  port: 8083

spring:
  application:
    name: restaurant-service

  datasource:
    url: jdbc:mysql://localhost:3306/restaurantdb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
